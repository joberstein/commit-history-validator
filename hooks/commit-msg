#!/usr/bin/env zx

import { parse } from "path";
import {validateCurrentCommit} from "../utils/commit.mjs";

const messagePath = process.argv[3];

const { name: hook } = parse(__filename);
console.info(`Running '${hook}' hook...`);

await validateCurrentCommit(messagePath);